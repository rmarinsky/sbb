plugins {
    id 'java-library'
    id 'maven-publish'
}

group 'ua.marinsky'
version '0.1'

repositories {
    mavenCentral()
}

publishing {
    publications {
        library(MavenPublication) {
            from components.java
        }
    }
    repositories {
        maven {
            url = uri("${buildDir}/publishing-repository")
        }
    }
}
